Register.
---
tags:
  - Record
consumes:
    - multipart/form-data
parameters:
    - in: formData
      name: token
      type: string
      require: True
      description: token.
    - in: query
      name: record_id
      type: int
      require: True
      description: record_id.
responses:
  200:
    description: Delete that record successfully
  500:
    description: Fail to delete that record